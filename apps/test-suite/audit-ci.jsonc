{
    "$schema": "https://github.com/IBM/audit-ci/raw/main/docs/schema.json",
    "low": true,
    "allowlist": [
        // GHSA-8cw4-87c7-c6xx (csv-parse):
        // Stable artillery 2.0.34 still depends on csv-parse 4.x; the
        // non-affected line is csv-parse 7.x, a multi-major jump outside
        // the stable upstream range. Wait for a stable artillery release
        // that depends on csv-parse >=7.0.2 and is past minimumReleaseAge.
        // Do not override across majors without compatibility approval.
        // Owner: team-engineering-reviewers. Tracking: create internal ticket.
        // Expires: 2026-10-07.
        "GHSA-8cw4-87c7-c6xx"
    ]
}
