import { BatchScrapeView } from './batch-scrape-view';

export const metadata = {
  title: 'Batch Scrape · Fire Enrich playground',
};

export default function BatchScrapePage() {
  return <BatchScrapeView />;
}
