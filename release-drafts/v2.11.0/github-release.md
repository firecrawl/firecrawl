# Firecrawl v2.11.0

## Improvements

- **`redactPII` option** — Added a `redactPII` option to `/v2/scrape`, `/v2/batch/scrape`, `/v2/crawl`, `/v2/parse`, and `/v2/extract`. Set `redactPII: true` and the worker routes the scraped markdown through fire-privacy and returns the redacted text as `document.markdown`. The object form accepts `mode: "accurate" | "aggressive" | "fast"`, an `entities[]` filter (`PERSON`, `EMAIL`, `PHONE_NUMBER`, `ADDRESS`, `SECRET`, ...), and `replaceStyle: "tag" | "mask" | "remove"`. Fail-soft: upstream timeouts and errors leave the scrape successful with the original markdown intact. Costs +4 credits per scrape when enabled (plus +4 per extra PDF page).
- **`redactPII` across SDKs** — Exposed the new `redactPII` option on scrape, batch-scrape, parse, crawl, and extract models in the JS, Python, Java, .NET, Go, PHP, Ruby, Rust, and Elixir SDKs.
- **`deterministicJson` format** — Added a new `deterministicJson` format on `/v2/scrape`, `/v2/batch/scrape`, `/v2/crawl`, `/v2/parse`, and `/v2/extract` that codegens a reusable JS extractor for the requested schema and runs it in a sandbox against the page, populating `document.json` like the existing `json` format but without a per-request LLM call. Extractors and their LLM responses are cached per site/schema so repeat scrapes amortize to near-zero LLM cost. Accepts an optional `schema` and `prompt`, billed at 10 credits per page when this run generated a fresh extractor and 3 credits per page when it reused a cached one, and cannot be combined with the `json` format on the same request.
- **Generic video discovery** — The `video` format now returns a structured `document.videos` array (alongside the existing legacy `document.video` string for supported providers like YouTube). Each entry carries `url`, `sourceURL`, `source`, and optional `kind`, `provider`, `title`, `thumbnail`, `description`, `duration`, `mimeType`, `width`, `height`, and `metadata`, populated from page HTML via the avgrab service. YouTube URLs continue to use the legacy provider path; non-provider pages now return direct video files, HLS manifests, and embeds without a download/upload round-trip. Configured via the `AVGRAB_SERVICE_URL` env var on self-host.
- **Crawl wall-clock timestamps** — `GET /v2/crawl/{id}` and `GET /v2/batch/scrape/{id}` now return `createdAt`, `completedAt`, and `duration` (seconds) so callers can read actual crawl wall-clock time directly from the API. `completedAt` is set on terminal states; in-progress crawls report `duration` as `createdAt → now`. Kickoff-failure crawls report a stable `duration: 0` and `completedAt = createdAt` instead of a value that grew on every poll.
- **`/v2/search/research` proxy** — Added typed research endpoints at `GET /v2/search/research/papers`, `/papers/:id`, `/papers/:id/similar`, and `/github`, with the legacy `/v2/research/*` mount kept as an alias that emits warning logs and duplicates renamed response fields in snake_case for backwards compatibility. Search billing applies (2 credits per 10 results; 10 credits per 10 results for ZDR teams). Exposed across the JS, Python, Java, .NET, Go, PHP, Ruby, Rust, and Elixir SDKs.
- **`/interact` is a `/browser` alias** — `POST /interact`, `GET /interact`, `POST /interact/:sessionId/execute`, and `DELETE /interact/:sessionId` are now full aliases for the `/v2/browser` session endpoints. Behavior, rate limits, and the 2-credit charge on session create are identical; existing `/v2/browser` callers keep working unchanged.
- **`cdpUrl` on `/interact` and `/v2/browser` execute** — The `POST /v2/scrape/:jobId/interact` and `/v2/browser` execute responses now include the raw CDP WebSocket URL alongside the existing iframe view URLs, so callers can attach Playwright, Puppeteer, or any CDP client directly to the live browser session. Surfaced as `cdpUrl` on the JS SDK and `cdp_url` on the Python SDK `BrowserExecuteResponse`.
- **Generic `/v2/feedback` endpoint** — Added a generic `POST /v2/feedback` route covering search, scrape, parse, and map jobs, with shared recording/refund logic and per-endpoint refund-policy snapshots. The legacy `POST /v2/search/:jobId/feedback` route keeps working and writes into the same `search_feedback` table with endpoint and job metadata for analytics.
- **Keyless `/v2/parse`** — `POST /v2/parse` is now reachable without an API key, subject to the per-IP keyless daily credit cap and rate limits, matching the keyless surface available on scrape and search.
- **Atomic keyless credit reservation** — Keyless requests now atomically reserve their projected credits in Redis before work starts (across v1/v2 scrape, search, parse, and interact) and reconcile to actual usage after completion, so concurrent multi-credit calls can no longer race past the daily cap.
- **Optional Spur IP reputation check** — Self-host deployments can enable an optional Spur Context IP reputation check on keyless requests to reject IPs flagged as proxy/VPN/datacenter abuse before they consume keyless credits. Off by default; configured via env var.
- **Monitor goals with LLM judge** — Added `goal` and `judgeEnabled` to monitors. When a goal is set, every changed page is run through an LLM judge that labels the diff `meaningful` or `noise` against the goal, drops alert spam, sorts meaningful pages first in summary emails, and renders the unified diff inline with color cues for up to five meaningful pages. Surfaced on monitor GET/responses and exposed in JS, Python, Java, .NET, Go, PHP, Ruby, Rust, and Elixir SDKs. Judge calls cost +1 credit per judged page; monthly estimates double when `judgeEnabled` is on.
- **Monitor JSON diffs** — Monitors that scrape with `{type: "json"}` or `{type: "changeTracking", modes: ["json"]}` now compute field-level JSON diffs against the previous run instead of diffing the rendered markdown. Each check persists a `snapshot.json` of current field values alongside the diff, available through `GET /v2/monitor/:id/checks/:checkId` and typed in the JS, Python, Java, .NET, Go, PHP, Ruby, and Rust SDKs.
- **Mixed-mode change tracking** — Monitors requesting both `["json", "git-diff"]` modes now run both diffs and report `changed` whenever either side changes, with the markdown unified diff attached as a sidecar on the JSON artifact. Previously this combination silently returned `same` when only one side moved.
- **Monitor `meaningfulChange` field** — Judge output now includes a `meaningfulChange` (and `meaningfulChanges[]` per-page) field carrying the verbatim diff text the judge flagged as meaningful, plus a per-change reason. Surfaced in monitor check pages, webhooks, and the JS, Python, Go, Java, .NET, PHP, Ruby, and Rust types.
- **Monitor email confirmation** — External monitor recipients now go through an opt-in confirmation flow with one-click unsubscribe; team members are auto-confirmed. `POST /v2/monitor/email/confirm` and `POST /v2/monitor/email/unsubscribe` accept a token in the request body, and `Monitor` responses include `emailRecipientSubscriptions` so callers can see who is `pending`, `confirmed`, or unsubscribed.
- **AM/PM monitor schedules** — Daily monitor schedules now accept 12-hour inputs like `daily at 9am`, `daily at 9:30am`, and `daily at 5:30pm`, and convert them to the correct UTC cron expression.
- **Monitor webhook delivery status** — `GET /v2/monitor/:id/checks` and `GET /v2/monitor/:id/checks/:checkId` now overlay the latest webhook delivery from `webhook_logs`, surfacing `attempted`, `delivered`, `statusCode`, `error`, and `deliveredAt` on each check.
- **Monitor default wait & timeout** — Monitor scrapes now default `waitFor` to 5 s and `timeout` to `2× waitFor`, clamped to 120 s, to cut false positives caused by partially-rendered pages. Defaults apply only when the caller does not pass `waitFor` or `timeout`.
- **Monitor judge timeout** — Per-attempt judge timeout raised from 8 s to 30 s to absorb slower model responses without flaky timeouts. Retry count and backoff are unchanged.
- **Monitor scheduler jitter** — Added a deterministic per-monitor offset to the scheduler claim so checks are staggered within their interval instead of all firing at the top of the minute, smoothing load spikes during heavy fan-out.
- **Dedicated search credits** — Search billing now meters against a dedicated `SEARCH_CREDITS` feature instead of the shared `CREDITS` pool used by scrape, crawl, and extract. Standalone scrapes still meter against `CREDITS`; scrapes initiated from a `/v2/search` request (via `scrapeOptions`) also bill against `CREDITS` since they are scrape work attached to a search. Reflected in `/v2/team/credit-usage` and the search-feedback refund path.
- **Agent discovery on `401`** — Auth-failed responses on `/v0`, `/v1`, and `/v2` endpoints now include a `WWW-Authenticate: Bearer realm="firecrawl"` header so MCP and agent clients can discover the credential scheme programmatically instead of hard-coding it.
- **`forced-zdr` and `forced-anon` search modes** — The team flag `searchZDR: "forced"` has been split into `"forced-zdr"` (always-on ZDR enterprise mode) and `"forced-anon"` (always-on anonymous routing); the previous `"forced"` value is kept as a deprecated alias for `"forced-zdr"`. The resolved mode now drives both billing and upstream routing. Search endpoints also accept teams with `searchZDR` forced, matching scrape.
- **Self-host Drizzle migration** — Migrated the API from the Supabase SDK to Drizzle ORM on native Postgres. Self-host deployments now configure `DATABASE_URL`, optional `DATABASE_REPLICA_URL`, and `INDEX_DATABASE_URL`; the old `SUPABASE_URL`, `SUPABASE_ANON_TOKEN`, `SUPABASE_SERVICE_TOKEN`, `SUPABASE_REPLICA_URL`, `INDEX_SUPABASE_URL`, `INDEX_SUPABASE_SERVICE_TOKEN`, and `SEARCH_INDEX_SUPABASE_URL` env vars are removed. Stripe auto-recharge has been removed; credit top-ups now flow through Autumn.
- **SemVer Docker image tags** — Production API images now publish to `ghcr.io/firecrawl/firecrawl` as immutable `X.Y.Z` SemVer manifests plus `X.Y`, `X`, and `latest` aliases. Self-host deployments should switch from the deprecated `vX.Y.Z` Docker tag to the bare `X.Y.Z` SemVer tag (or `X.Y` / `X` / `latest`); staging images publish under `X.Y.Z-staging` plus `firecrawl-staging:latest`.
- **`PDF size cap`** — Raised the PDF download and scrape cap from 30 MB to 50 MB.
- **X/Twitter scrape billing** — Updated X/Twitter scrapes to 29 credits per request (up from 9) to reflect the cost of the underlying provider, and post bodies are now extracted as escaped markdown so links and formatting survive the scrape.
- **JS SDK string API key** — `new Firecrawl("fc-key")` now works alongside `new Firecrawl({ apiKey: "fc-key" })`. The constructor accepts a bare string and trims whitespace; whitespace-only keys are rejected with a clear error.
- **JS SDK V2 prototype discoverability** — V2 methods like `scrape`, `search`, `crawl`, `map`, and `parse` are now installed on `Firecrawl.prototype` at module load so agent runtimes that introspect via `Object.getPrototypeOf(app)` can see them.
- **V1 method aliases on V2 SDKs** — Added deprecated `scrape_url`/`scrapeUrl`, `crawl_url`/`crawlUrl`, and matching aliases on V2 Python and JS clients so callers trained on V1 docs keep working instead of hitting `AttributeError`/`TypeError`. Aliases match the V1 `id` parameter name on status/error helpers.
- **Python SDK `crawl()` scrape kwargs** — `crawl()` and `start_crawl()` (sync and async) now accept the same direct kwargs as `scrape()` and `batch_scrape()` (`formats`, `headers`, `include_tags`, `exclude_tags`, etc.), removing the need to wrap them in `ScrapeOptions(...)`. `start_crawl()` now also forwards `timeout`.
- **`SearchData.data` guidance** — Accessing `.data` on a search result now raises a clear error pointing at `.web`, `.news`, and `.images` with their counts, so LLM agents that hallucinate `.data` get an actionable message instead of a silent `None`.
- **Python `JsonFormat`/`ChangeTrackingFormat` defaults** — `JsonFormat(prompt="...")` and `ChangeTrackingFormat(modes=[...])` no longer require an explicit `type=` argument; both now default `type` like `ScreenshotFormat` does.
- **`ChangeTrackingFormat` case acceptance** — SDKs now accept both `change_tracking` and `changeTracking` for the format `type` field so payloads round-trip between snake_case and camelCase clients without re-mapping.
- **Python SDK monitor `goal` / `judge_enabled`** — `FirecrawlClient.create_monitor()` and `update_monitor()` (sync and async) accept `goal` and `judge_enabled` kwargs directly, matching the new `/v2/monitor` request shape.

## Fixes

- Resolved multiple CVEs across the API and SDKs by upgrading `axios` to `1.18.0` in `apps/api`, `apps/js-sdk`, and `apps/js-sdk/firecrawl` (GHSA-35jp-ww65-95wh, GHSA-654m-c8p4-x5fp, GHSA-898c-q2cr-xwhg, GHSA-pjwm-pj3p-43mv), pinning `@opentelemetry/core` to `>=2.8.0` (GHSA-8988-4f7v-96qf), bumping the AI SDK family to non-vulnerable `@ai-sdk/provider-utils` (GHSA-866g-f22w-33x8), upgrading `esbuild` to `0.28.1` (GHSA-gv7w-rqvm-qjhr, GHSA-g7r4-m6w7-qqqr), the Rust SDK's `openssl` to `0.10.80` (CVE-2026-41676), `@grpc/grpc-js` in the test-suite, `@google-cloud/storage` to `^7.21.0` in `apps/api`, and pnpm-overriding `shell-quote` (GHSA-w7jw-789q-3m8p), `ws` to `8.21.0` (GHSA-58qx-3vcg-4xpx), `brace-expansion` to `5.0.6` (GHSA-jxxr-4gwj-5jf2), `qs` (GHSA-Q8MJ-M7CP-5Q26 / CVE-2026-8723), `uuid` to `11.1.1` (GHSA-W5HQ-G745-H8PQ), `js-cookie` (GHSA-QJX8-664M-686J), `form-data`, `js-yaml`, `protobufjs` to `7.6.4`, `vite` to `7.3.5`, and `@babel/core` to `7.29.7` across `apps/api`, `apps/js-sdk`, `apps/playwright-service-ts`, `apps/test-suite`, and `apps/ui/ingestion-ui`.
- Fixed scrape-worker pods freezing the Node event loop for tens of seconds when the LLM extractor encoded very large inputs through `@dqbd/tiktoken` — token trimming now pre-bounds the input by character count and trims in a single encode/decode pass, eliminating nuq lock expiry, duplicate finish-crawl jobs, and Kubernetes liveness-probe restarts caused by the synchronous re-encode loop.
- Fixed Wikipedia scrapes missing the `og:image` meta tag (and therefore `metadata.ogImage`) on roughly half of Wikimedia URLs — the image URL is now synthesized into the engine's `<head>` from the Wikimedia Enterprise API's `article.image.content_url`.
- Fixed `cancel` on `/v1` and `/v2` crawls and batches not draining the per-team concurrency-limit backlog — queued job IDs are now removed via a chunked Redis pipeline and `status` reports `cancelled` immediately, even while the worker group is still draining.
- Fixed `POST /v2/crawl/:id/cancel` reporting stale state by reading the canonical job state from NuQ instead of Redis.
- Fixed crawls being silently capped at 0 when a team had 0 credits but Autumn allowed the request via overage — `crawlerOptions.limit` now honors the caller's value when Autumn returns `{ allowed: true }`.
- Fixed credit checks falling back to legacy ACUC accounting when the Autumn API failed — Autumn is now the source of truth and a failed check returns an error instead of silently allowing requests on diverged ACUC balances.
- Fixed `/v1/team/credit-usage` and `/v2/team/credit-usage` reporting `remaining: 0` for teams in overage — the response now derives `remaining` from `granted - usage` so overage shows as a signed negative balance.
- Fixed monitor checks being charged when credit lock requests were denied — denied locks now mark the check `skipped_no_credits` and stop the run, while preview teams and transient billing-API failures proceed without a lock so monitoring keeps working.
- Fixed `/v1/crawl` and `/v2/crawl` accepting broken `delay` values (non-numeric, negative, or greater than 1 day) — these are now rejected with a clear validation error instead of being applied silently.
- Fixed search-log inserts failing on certain query strings by sanitizing non-UTF8 characters before writing them to Postgres.
- Fixed NuQ workers crashing on AMQP connection close by handling channel/connection shutdown explicitly instead of treating it as an unexpected failure, and corrected RabbitMQ property-name mapping that was breaking message acks on certain queues.
- Fixed sync `Firecrawl.search()` in the Python SDK raising `TypeError: ... unexpected keyword argument 'include_domains'` when callers passed `include_domains` or `exclude_domains` — the sync signature now matches the async client and the wire payload.
- Fixed JSON-mode monitor diffs returning spurious `changed` verdicts when the underlying scrape returned identical field values in a different order — diffs now use deep, order-insensitive equality.
- Fixed JSON-mode monitors treating an empty-string markdown scrape as missing input — pages that legitimately render no text now report `same` on subsequent runs instead of staying perpetually `changed`.
- Fixed monitor webhooks being dispatched twice for the same check by gating notifications on a per-check Redis NX claim (7-day TTL) across run, stale, and reconcile paths.
- Fixed monitor webhooks being dead-lettered as `malformed_json` by the Rust dispatcher — `monitor.page` and `monitor.check.completed` payloads now wrap `data` in an array, matching the existing crawl/batch shape.
- Fixed the monitor judge fabricating before/after text by passing only the rendered markdown diff as evidence and tightening the prompt to require quotes from that diff.
- Fixed the monitor judge missing context on long pages by removing input truncation — the full unified diff and page content are now sent to the judge instead of being clipped.
- Hardened monitor check responses against corrupt or unexpected artifact payloads in GCS — bad data now surfaces as no diff instead of breaking `GET /v2/monitor/:id/checks/:checkId`.
- Fixed PDF size-cap enforcement being bypassed on certain scrape paths so PDFs over the limit are now rejected consistently, and fire-engine now classifies "file size exceeds limit" as a structured rejection instead of a generic failure.
- Fixed transient GCS write failures by treating `503` responses as a slowdown signal rather than a hard failure, improving job-log upload success rates under load.
- Fixed `mostRecentSuccess` always returning `null` from `GET /v1/team/queue-status` and `GET /v2/team/queue-status` in production deployments where `REDIS_EVICT_URL` and `REDIS_URL` resolve to different instances — the controllers now read the key from the same Redis the scrape worker writes it to.
- Updated the native PDF inspector dependency to the published `pdf-inspector 0.1.0` crate so PDF table extraction picks up the latest upstream fixes without depending on a git revision.
- Fixed missing or blank header rows on HTML tables whose first rectangular row is encoded with `td` cells — the markdown converter now promotes that row to a header before conversion so column labels survive into `document.markdown` and are visible to downstream redaction flows.
- Fixed `ChangeTrackingFormat` options being dropped through Python SDK serialization round-trips — `modes`, `prompt`, and related fields now survive `model_dump()` / re-instantiation.

## API

- Added `redactPII: boolean | { mode?: "accurate" | "aggressive" | "fast", entities?: string[], replaceStyle?: "tag" | "mask" | "remove" }` to `POST /v2/scrape`, `POST /v2/batch/scrape`, `POST /v2/crawl`, `POST /v2/parse`, and `POST /v2/extract`. When enabled, `document.markdown` is replaced with the redacted markdown. Defaults are `mode: "accurate"` and `replaceStyle: "tag"`. New env vars `FIRE_PRIVACY_URL` and `FIRE_PRIVACY_TIMEOUT_MS` (default `5000`) control the upstream call.
- Removed the experimental `pii` scrape format and the `document.pii` diagnostics block (`status`, `spans`, `counts`, `redactedMarkdown`) from request schemas and SDK types. Requests that include `"pii"` in `formats` are now rejected by the schema. Use `redactPII` to get safe output in `document.markdown`.
- Added the `deterministicJson` format (`{ type: "deterministicJson", schema?, prompt? }`) to `POST /v2/scrape`, `POST /v2/batch/scrape`, `POST /v2/crawl`, `POST /v2/parse`, and `POST /v2/extract`. Populates `document.json` like the `json` format. Cannot be combined with the `json` format in the same request. New self-host env vars `EXTRACT_CODEGEN_MODEL`, `EXTRACT_ANCHOR_MODEL`, `EXTRACT_LIGHT_MODEL`, and `CODE_SANDBOX_URL` (default `ws://code-sandbox:3001`) configure the extractor pipeline.
- Added `document.videos: VideoItem[]` to `POST /v2/scrape` (and the endpoints that share its options) when the `video` format is requested. Each `VideoItem` has `url`, `sourceURL`, `source`, and optional `kind`, `provider`, `title`, `thumbnail`, `description`, `duration`, `mimeType`, `width`, `height`, and `metadata`. The legacy `document.video` string is still populated for supported providers (e.g. YouTube). New self-host env var `AVGRAB_SERVICE_URL` configures the upstream video discovery service.
- Added `createdAt`, `completedAt`, and `duration` (seconds) to `GET /v2/crawl/{id}` and `GET /v2/batch/scrape/{id}` responses. `completedAt` is only present on terminal states; `duration` is `createdAt → completedAt` on terminal states or `createdAt → now` while in progress, and is stable at `0` on kickoff-failure crawls.
- Added a `WWW-Authenticate: Bearer realm="firecrawl"` header on all `401 Unauthorized` responses across `/v0`, `/v1`, and `/v2` endpoints to advertise the credential scheme for agent discovery.
- Added `POST /interact`, `GET /interact`, `POST /interact/:sessionId/execute`, and `DELETE /interact/:sessionId` as full aliases for the matching `/v2/browser` session endpoints. Behavior, rate limits, and the 2-credit charge on session create are identical.
- Added `cdpUrl` to the `POST /v2/scrape/:jobId/interact` and `/v2/browser` execute responses, exposing the raw CDP WebSocket URL alongside `liveViewUrl` and `interactiveLiveViewUrl`. Surfaced as `cdpUrl` (JS SDK) and `cdp_url` (Python SDK) on `BrowserExecuteResponse`.
- Added `POST /v2/feedback` covering search, scrape, parse, and map jobs with shared recording and refund logic and per-endpoint refund-policy snapshots. Both this route and the legacy `POST /v2/search/:jobId/feedback` write into the existing `search_feedback` table with endpoint and job metadata.
- Added the `/v2/search/research` proxy with `GET /v2/search/research/papers`, `/papers/:id`, `/papers/:id/similar`, and `/github`. The legacy `/v2/research/*` mount is kept as a deprecated alias that emits warning logs and includes snake_case duplicates of renamed response fields. Billed against `SEARCH_CREDITS` at 2 credits per 10 results (10 credits per 10 results for ZDR teams). Configured via `RESEARCH_PROXY_URL` on self-host. Available on the JS, Python, Java, .NET, Go, PHP, Ruby, Rust, and Elixir SDKs.
- Added keyless support to `POST /v2/parse` (subject to the per-IP keyless daily credit cap), and changed keyless billing across `/v1` and `/v2` scrape, search, parse, and interact to atomically reserve projected credits before work starts and reconcile to actual usage on completion.
- Added new `searchZDR` values `"forced-zdr"` and `"forced-anon"` and deprecated `"forced"` (still accepted as an alias for `"forced-zdr"`). The resolved mode is now used for both billing and upstream routing, and search endpoints accept teams whose flags resolve to a forced ZDR mode.
- Added `goal: string|null` and `judgeEnabled: boolean` to `POST /v2/monitor` and `PATCH /v2/monitor/:id`. `judgeEnabled` defaults to `true` when `goal` is non-empty; passing `goal: null` clears it.
- Added `judgment` and `meaningfulChange` to monitor check pages on `GET /v2/monitor/:id/checks/:checkId`, plus a per-change `reason` and the `meaningfulChanges[]` array. Typed in the JS, Python, Java, .NET, Go, PHP, Ruby, Rust, and Elixir SDKs.
- Added a structured `diff` object (`text` and/or `json`) and a `snapshot` field to `MonitorCheckPage`. JSON-mode checks return field-level diffs plus a current-value snapshot; markdown-mode checks continue to return a text diff.
- Added `POST /v2/monitor/email/confirm` and `POST /v2/monitor/email/unsubscribe`. Both are unauthenticated, accept the token only in the request body (query-string tokens are rejected), and use `invalid_token` (400) and `not_found` (404) error codes.
- Added `emailRecipientSubscriptions` to `Monitor` responses on `GET /v2/monitor/:id` and on create/update. Each entry reports `email`, `status` (`pending` | `confirmed` | `unsubscribed`), `source`, and `confirmationEmailSent`.
- Added `billing_status: "skipped_no_credits" | "not_applicable"` to monitor check responses to disambiguate runs that were skipped because Autumn denied the credit lock from runs that proceeded without a billing call (preview teams, transient billing failure).
- Changed the monitor webhook payload so `monitor.page` and `monitor.check.completed` events wrap `data` in an array. `monitor.page` now includes `isMeaningful` (`boolean | null` for changed pages), `judgment`, and a `diff` object with `text` and/or `json`.
- Added `origin: string` to monitor create/update bodies, matching every other v2 endpoint; SDKs now auto-inject `origin` on these calls.
- Added 12-hour AM/PM parsing for daily monitor schedules — strings like `9:30am` and `5pm` are accepted and converted to 24-hour cron.
- Added stricter validation on the `delay` parameter for `POST /v2/crawl` and `POST /v1/crawl` — non-numeric, negative, or values greater than `86400` are rejected with a clear schema error instead of being silently applied.
- Normalized monitor `scrapeOptions.formats` so `{type: "changeTracking", modes: ["json"]}` is rewritten to `{type: "json"}` before scraping, and the mixed-mode `["json", "git-diff"]` form runs both diffs instead of silently falling back to one.
- Added `include_domains` and `exclude_domains` keyword arguments to the Python SDK's sync `Firecrawl.search()` to bring it in line with `AsyncFirecrawl.search()` and the `/v2/search` wire payload.
- Added V1-compatible method aliases (`scrape_url`/`scrapeUrl`, `crawl_url`/`crawlUrl`, `batch_scrape_urls`/`batchScrapeUrls`, `extract`, `map_url`/`mapUrl`, and their status/error helpers) on V2 Python and JS clients. Aliases emit a `DeprecationWarning`.
- Changed `/v2/search` billing to meter against a dedicated `SEARCH_CREDITS` Autumn feature (separate from `CREDITS`). Scrapes initiated inside a search request still bill against `CREDITS` under their own endpoint. `/v2/team/credit-usage` returns the search balance under the new feature.
- Changed X/Twitter scrape billing from 9 to 29 credits per scrape.
- Changed `/v2/scrape` (and the endpoints that share its options) to charge +4 credits when `redactPII` is enabled, plus +4 credits per extra PDF page on top of the existing +1/page parse cost. The `deterministicJson` format bills 10 credits per page when this run generates a fresh extractor and 3 credits per page when it reuses a cached one (replaces the earlier flat 7); `json` (including `changeTracking` json mode) remains 5 credits per page.
- Changed monitor billing: each judged changed page now consumes +1 credit when `judgeEnabled` is on, and monthly credit estimates double when `judgeEnabled` is enabled at create/update. Monitor runs that fail to lock credits now return `skipped_no_credits` instead of charging.
- Removed the Stripe credit auto-recharge flow on the API — credit top-ups are now driven through Autumn. Self-host deployments must rely on Autumn or their own billing pipeline.
- Removed the legacy `SUPABASE_URL`, `SUPABASE_ANON_TOKEN`, `SUPABASE_SERVICE_TOKEN`, `SUPABASE_REPLICA_URL`, `INDEX_SUPABASE_URL`, `INDEX_SUPABASE_SERVICE_TOKEN`, and `SEARCH_INDEX_SUPABASE_URL` environment variables. Self-host deployments must configure `DATABASE_URL`, optional `DATABASE_REPLICA_URL`, and `INDEX_DATABASE_URL` instead.
- Changed the production API Docker image to publish under `ghcr.io/firecrawl/firecrawl` as immutable `X.Y.Z` SemVer manifests plus `X.Y`, `X`, and `latest` aliases; staging images publish as `X.Y.Z-staging` plus `firecrawl-staging:latest`. The legacy `vX.Y.Z` Docker tag is no longer published — pull `X.Y.Z` (or `X.Y` / `X` / `latest`) instead. Git tags are unchanged.

---

**Full Changelog**: https://github.com/firecrawl/firecrawl/compare/v2.10...v2.11.0
