name: Deploy Playwright Image to GHCR

on:
  push:
    branches:
      - release
    paths:
      - apps/playwright-service-ts/**
      - .github/workflows/deploy-playwright.yml
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    defaults:
      run:
        working-directory: './apps/playwright-service-ts'
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lowercase repo owner
        run: echo "REPO_OWNER=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_ENV"
        env:
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./apps/playwright-service-ts
          push: true
          tags: ghcr.io/${{ env.REPO_OWNER }}/playwright-service:latest
          provenance: false
