name: .NET SDK Test Suite

on:
  pull_request:
    branches:
      - main
    paths:
      - apps/dot-net-sdk/**
      - .github/workflows/test-dotnet-sdk.yml
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build and Test
    runs-on: self-hosted
    env:
      DOTNET_NOLOGO: "1"
      DOTNET_CLI_TELEMETRY_OPTOUT: "1"
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'pull_request' ||
      (github.event_name == 'push' && github.ref == 'refs/heads/main')
    steps:
      - uses: actions/checkout@v5

      - name: Set up .NET 8
        uses: actions/setup-dotnet@v4
        env:
          # runner.temp is only valid at step-level, not job-level env.
          # Redirect setup-dotnet away from /usr/share/dotnet which the
          # self-hosted runner user can't write to.
          DOTNET_INSTALL_DIR: ${{ runner.temp }}/.dotnet
        with:
          dotnet-version: "8.0.x"

      - name: Restore dependencies
        working-directory: ./apps/dot-net-sdk
        run: dotnet restore

      - name: Build
        working-directory: ./apps/dot-net-sdk
        run: dotnet build --no-restore --configuration Release

      - name: Run unit tests
        working-directory: ./apps/dot-net-sdk
        run: dotnet test --no-build --configuration Release --logger "trx;LogFileName=test-results.trx" --results-directory ./TestResults

      - name: Publish test report
        if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: dorny/test-reporter@3eeb9fc888e82e8be2fb356bbeec2750231672bc # v1
        with:
          name: .NET SDK Test Report
          path: apps/dot-net-sdk/TestResults/**/*.trx
          reporter: dotnet-trx
          fail-on-error: true
